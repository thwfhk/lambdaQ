-- candyqwq : Circ(1, bit ⊗ bit)
fun candyqwq = / () : One .
  q <- gate init0 ();
  a <- gate init0 ();
  (q, a) <- gate CNOT (q, a);
  q <- gate H q;
  x <- gate meas q;
  y <- gate meas a;
  output (x,y)